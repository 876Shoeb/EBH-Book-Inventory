@model BookInventory.Entities.Condition

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Condition</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.DescriptiveName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.DescriptiveName)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-primary" asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> |
    <a class="btn btn-primary hBack" asp-action="Index">Back to List</a>
</div>
